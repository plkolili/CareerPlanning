# VSCode 详细使用教程 💻

> Visual Studio Code（简称 VSCode）是目前最受欢迎的代码编辑器之一。它免费、开源、跨平台，拥有丰富的插件生态，是编程学习的最佳伴侣。

---

## 一、认识 VSCode

### 1.1 为什么选择 VSCode？

| 特性       | 说明                           |
| ---------- | ------------------------------ |
| 🆓 免费开源 | 完全免费，由微软维护，社区活跃 |
| 🖥️ 跨平台   | Windows、macOS、Linux 都能用   |
| ⚡ 轻量快速 | 启动快，占用资源少             |
| 🔌 插件丰富 | 上万款插件，满足各种开发需求   |
| 🤖 智能提示 | 代码补全、错误检查、跳转定义   |
| 🐛 内置调试 | 无需额外配置即可调试代码       |
| 📦 内置 Git | 图形化版本控制，操作简单       |

### 1.2 下载与安装

1. **官网下载**：访问 [https://code.visualstudio.com/](https://code.visualstudio.com/)
2. **选择版本**：点击对应系统的下载按钮
   - Windows: 下载 `.exe` 安装包
   - macOS: 下载 `.dmg` 安装包
   - Linux: 下载 `.deb` 或 `.rpm` 包
3. **安装**：
   - **Windows**: 双击安装包，建议勾选"添加到 PATH"和"添加到右键菜单"
   - **macOS**: 将应用拖到"应用程序"文件夹
   - **Linux**: 双击安装包或使用命令行安装

### 1.3 首次启动配置

首次打开 VSCode 时：

1. **选择主题**：推荐选择"深色"主题（对眼睛友好）
2. **安装中文语言包**（可选）：
   - 按 `Ctrl+Shift+P` 打开命令面板
   - 输入 `Configure Display Language`
   - 选择 `Install additional languages`
   - 安装"Chinese (Simplified) Language Pack"
   - 重启 VSCode

---

## 二、界面导览

打开 VSCode 后，你会看到以下界面布局：

```
┌─────────────────────────────────────────────────────────────┐
│  文件  编辑  选择  查看  ... [顶部菜单栏]                      │
├──────────┬─────────────────────────────────────────┬────────┤
│          │                                         │        │
│   📁     │                                         │   🔧   │
│   🔍     │                                         │   🐛   │
│   🌿     │          📝 编辑区域                     │   🔄   │
│   🐞     │          (写代码的地方)                   │        │
│   🧩     │                                         │   ⬇️   │
│          │                                         │  侧边  │
│  活动栏   │                                         │        │
│  (最左侧) │                                         │        │
│          │                                         │        │
├──────────┴─────────────────────────────────────────┴────────┤
│  > 终端                                                      │
│  $ _                                                        │
│  [底部面板 - 终端/输出/调试控制台/问题]                        │
├─────────────────────────────────────────────────────────────┤
│  UTF-8 | Markdown | 行 12, 列 34 | [状态栏]                  │
└─────────────────────────────────────────────────────────────┘
```

### 2.1 活动栏（最左侧）

| 图标 | 名称       | 快捷键         | 功能             |
| ---- | ---------- | -------------- | ---------------- |
| 📁    | 资源管理器 | `Ctrl+Shift+E` | 管理文件和文件夹 |
| 🔍    | 搜索       | `Ctrl+Shift+F` | 全局搜索文件内容 |
| 🌿    | 源代码管理 | `Ctrl+Shift+G` | Git 版本控制     |
| 🐞    | 运行和调试 | `Ctrl+Shift+D` | 调试代码         |
| 🧩    | 扩展       | `Ctrl+Shift+X` | 安装插件         |
| 👤    | 账户       | -              | 同步设置和登录   |

### 2.2 侧边栏

根据活动栏的选择，侧边栏会显示不同内容：
- **资源管理器**：显示当前文件夹结构
- **搜索**：显示搜索结果
- **源代码管理**：显示 Git 更改
- **调试**：显示调试信息

### 2.3 编辑区域

- **标签页**：可同时打开多个文件，点击切换
- **分屏**：将编辑器分成左右/上下多个区域
- **面包屑**：显示当前文件路径和符号导航

### 2.4 面板（底部）

- **终端**：集成命令行终端
- **输出**：显示扩展的输出信息
- **调试控制台**：调试时的输出
- **问题**：显示代码错误和警告

### 2.5 状态栏（最底部）

显示当前文件信息：
- 行号和列号
- 文件编码（UTF-8）
- 文件类型（Markdown、Python 等）
- 语言模式
- Git 分支（如果在 Git 仓库中）

---

## 三、基础操作

### 3.1 打开文件夹（工作区）

VSCode 以"文件夹"为单位工作，而不是单个文件：

**方法一：菜单**
1. 点击菜单栏 `文件` → `打开文件夹...`
2. 选择你的项目文件夹

**方法二：拖拽**
- 直接将文件夹拖到 VSCode 窗口中

**方法三：命令行**
```bash
code /path/to/your/folder
```

> 💡 **提示**：打开文件夹后，VSCode 会记住这个"工作区"，下次打开会恢复上次的状态。

### 3.2 文件操作

| 操作     | 方法                              | 快捷键          |
| -------- | --------------------------------- | --------------- |
| 新建文件 | 资源管理器空白处右键 → `新建文件` | `Ctrl+N`        |
| 保存文件 | 菜单 `文件` → `保存`              | `Ctrl+S`        |
| 另存为   | 菜单 `文件` → `另存为...`         | `Ctrl+Shift+S`  |
| 关闭文件 | 点击标签页 ×                      | `Ctrl+W`        |
| 关闭所有 | 菜单 `文件` → `关闭文件夹`        | `Ctrl+K Ctrl+W` |

### 3.3 快速打开文件

当项目文件很多时，不要一层层找文件夹：

**方法：使用 "转到文件"**
1. 按 `Ctrl+P`（Mac: `Cmd+P`）
2. 输入文件名的一部分
3. 按 `Enter` 打开，或 `→` 预览

> 🎯 **示例**：输入 `main.py` 即可快速找到并打开该文件

### 3.4 搜索与替换

#### 当前文件内搜索
- `Ctrl+F`：打开搜索框
- `Ctrl+H`：打开替换框
- `F3` / `Shift+F3`：下一个/上一个匹配

#### 跨文件搜索（全局搜索）
1. 点击左侧 🔍 图标，或按 `Ctrl+Shift+F`
2. 输入搜索内容
3. 搜索结果会显示每个匹配的文件和行号

#### 高级搜索技巧
- 使用 `.*` 按钮开启正则表达式搜索
- 使用 `Aa` 按钮区分大小写
- 使用 `""` 按钮匹配整个单词

---

## 四、高效编辑技巧

### 4.1 多光标编辑（超级实用！）

多光标让你同时在多处编辑，是提高效率的神器。

#### 方法一：Alt + 点击
- 按住 `Alt` 键，在多个位置点击，即可创建多个光标
- 然后输入内容，会同时在所有光标位置出现

#### 方法二：Ctrl + D（选择下一个）
1. 选中一个单词
2. 按 `Ctrl+D`，会选中下一个相同的单词
3. 继续按 `Ctrl+D` 可以继续选中更多
4. 直接输入即可同时修改所有选中的地方

#### 方法三：Ctrl + Shift + L（全选所有）
- 选中一个单词后，按 `Ctrl+Shift+L`
- 会选中文件中所有相同的单词

#### 方法四：Alt + Shift + 鼠标拖拽（列选择）
- 按住 `Alt+Shift`，然后鼠标向下拖拽
- 可以创建一列垂直的光标

> 💡 **实战示例**：把一组变量名统一添加前缀 `old_`
> 1. 选中第一个变量名
> 2. 多次按 `Ctrl+D` 选中所有变量名
> 3. 按 `Home` 键跳到行首
> 4. 输入 `old_`

### 4.2 代码折叠与展开

当文件很长时，可以折叠不需要看的部分：

| 操作         | 快捷键          |
| ------------ | --------------- |
| 折叠当前区域 | `Ctrl+Shift+[`  |
| 展开当前区域 | `Ctrl+Shift+]`  |
| 折叠全部     | `Ctrl+K Ctrl+0` |
| 展开全部     | `Ctrl+K Ctrl+J` |

> 💡 **提示**：点击行号左侧的 `▼` 和 `▶` 箭头也可以折叠/展开

### 4.3 代码格式化

让代码自动变得整齐美观：

- **格式化当前文件**：`Shift+Alt+F`
- **格式化选中代码**：先选中代码，再 `Ctrl+K Ctrl+F`

> ⚠️ **注意**：某些语言需要安装对应的插件才能格式化（如 Python 需要安装 Black 或 autopep8）

### 4.4 智能提示（IntelliSense）

VSCode 会根据你写的代码提供智能建议：

- **触发提示**：`Ctrl+Space`
- **查看参数**：鼠标悬停在函数上
- **跳转到定义**：`F12`
- **查看引用**：`Shift+F12`
- **重命名符号**：`F2`（安全地重命名变量/函数）

### 4.5 代码片段（Snippets）

代码片段是预定义的代码模板，输入简短的触发词，按 `Tab` 即可展开：

| 语言       | 输入     | 效果                              |
| ---------- | -------- | --------------------------------- |
| Python     | `ifmain` | 展开 `if __name__ == "__main__":` |
| Python     | `for`    | 展开 for 循环模板                 |
| HTML       | `!`      | 展开 HTML5 文档模板               |
| JavaScript | `log`    | 展开 `console.log()`              |

---

## 五、扩展插件生态

### 5.1 安装插件

1. 点击左侧 🧩 图标，或按 `Ctrl+Shift+X`
2. 在搜索框输入插件名称
3. 点击 `安装` 按钮
4. 安装完成后可能需要重启 VSCode

### 5.2 推荐必装插件

#### 🌐 基础增强
| 插件名                             | 作用                          |
| ---------------------------------- | ----------------------------- |
| Chinese (Simplified) Language Pack | 中文语言包                    |
| Settings Sync                      | 同步设置到云端（VSCode 内置） |

#### 🐍 Python 开发
| 插件名        | 作用                              |
| ------------- | --------------------------------- |
| Python        | Python 语言支持（微软官方）       |
| Pylance       | Python 语言服务器，提供更好的提示 |
| Python Indent | 自动缩进修正                      |
| Jupyter       | 支持 Jupyter Notebook             |

#### 🎨 代码美化
| 插件名                   | 作用           |
| ------------------------ | -------------- |
| Prettier                 | 代码格式化神器 |
| Bracket Pair Colorizer 2 | 彩虹括号匹配   |
| indent-rainbow           | 缩进彩虹色显示 |

#### 🛠️ 效率工具
| 插件名              | 作用                       |
| ------------------- | -------------------------- |
| Auto Rename Tag     | 自动重命名 HTML/XML 标签对 |
| Path Intellisense   | 路径自动补全               |
| Code Runner         | 一键运行代码               |
| Markdown All in One | Markdown 增强              |

#### 🌙 主题推荐
| 插件名              | 风格                 |
| ------------------- | -------------------- |
| One Dark Pro        | 经典暗色主题         |
| Dracula Official    | 吸血鬼主题（紫色系） |
| Material Icon Theme | 文件图标美化         |

### 5.3 管理插件

- **禁用插件**：在扩展列表中找到插件，点击"禁用"
- **卸载插件**：点击"卸载"按钮
- **更新插件**：在扩展列表顶部查看可更新的插件

---

## 六、集成终端

VSCode 内置了终端，无需切换到其他窗口。

### 6.1 打开终端

- **打开/关闭终端**：`` Ctrl+` ``（反引号，在 Tab 键上方）
- **新建终端**：`Ctrl+Shift+`` `
- **从菜单打开**：`终端` → `新建终端`

### 6.2 终端操作

| 操作         | 快捷键                       |
| ------------ | ---------------------------- |
| 新建终端     | `Ctrl+Shift+``               |
| 关闭当前终端 | 输入 `exit` 或点击垃圾桶图标 |
| 切换终端     | 点击终端面板右上角的下拉菜单 |
| 分屏终端     | 点击终端面板右上角的分屏图标 |

### 6.3 终端配置

- **选择默认 Shell**：
  1. 点击终端面板右上角的 `+` 旁边的下拉箭头
  2. 选择"选择默认配置文件"
  3. 选择 PowerShell、Command Prompt、Git Bash 等

- **在特定文件夹打开终端**：
  - 在资源管理器中右键文件夹 → `在集成终端中打开`

---

## 七、调试功能

VSCode 的调试功能非常强大，无需额外配置即可调试多种语言。

### 7.1 基础调试流程

以 Python 为例：

1. **设置断点**：在代码行号左侧点击，出现红点
2. **启动调试**：按 `F5` 或点击左侧 🐞 图标 → "运行和调试"
3. **调试控制**：
   - `F5`：继续运行到下一个断点
   - `F10`：单步跳过（Step Over）
   - `F11`：单步进入（Step Into）
   - `Shift+F11`：单步跳出（Step Out）
   - `Shift+F5`：停止调试

### 7.2 调试面板

调试时，左侧会显示：
- **变量**：当前作用域内的变量及其值
- **监视**：添加表达式监视（如 `i + j`）
- **调用栈**：函数调用层次
- **断点**：所有设置的断点列表

### 7.3 调试配置

对于复杂的项目，需要创建调试配置：

1. 点击左侧 🐞 图标
2. 点击"创建 launch.json 文件"
3. 选择环境（Python、Node.js 等）
4. 根据需要修改配置

---

## 八、设置与个性化

### 8.1 打开设置

- **图形界面**：`Ctrl+,`（逗号）
- **JSON 文件**：`Ctrl+Shift+P` → 输入 `Preferences: Open Settings (JSON)`

### 8.2 常用设置项

搜索以下关键词进行设置：

| 设置项           | 建议值                  | 作用                   |
| ---------------- | ----------------------- | ---------------------- |
| `Font Size`      | 16                      | 编辑器字体大小         |
| `Font Family`    | 'Fira Code', 'Consolas' | 字体（推荐 Fira Code） |
| `Word Wrap`      | on                      | 自动换行               |
| `Tab Size`       | 4                       | Tab 键的空格数         |
| `Auto Save`      | afterDelay              | 自动保存（延迟后）     |
| `Format On Save` | ✓                       | 保存时自动格式化       |

### 8.3 更换主题

1. `Ctrl+K Ctrl+T` 打开主题选择
2. 或 `Ctrl+Shift+P` → `Color Theme`
3. 使用方向键预览，回车确认

### 8.4 文件图标主题

1. `Ctrl+Shift+P` → `File Icon Theme`
2. 选择喜欢的图标主题（推荐 Material Icon Theme）

### 8.5 自定义快捷键

1. `Ctrl+K Ctrl+S` 打开快捷键设置
2. 搜索要修改的命令
3. 双击快捷键，按新的组合键
4. 或右键 → `更改键绑定`

---

## 九、常用快捷键速查表

### 📝 编辑

| 快捷键             | 功能               |
| ------------------ | ------------------ |
| `Ctrl+X`           | 剪切行（未选中时） |
| `Ctrl+C`           | 复制行（未选中时） |
| `Ctrl+Shift+K`     | 删除行             |
| `Ctrl+Enter`       | 下方插入行         |
| `Ctrl+Shift+Enter` | 上方插入行         |
| `Alt+↑` / `Alt+↓`  | 移动当前行上下     |
| `Ctrl+/`           | 注释/取消注释      |
| `Ctrl+Shift+\`     | 跳转到匹配的括号   |

### 🔍 导航

| 快捷键         | 功能                  |
| -------------- | --------------------- |
| `Ctrl+P`       | 转到文件              |
| `Ctrl+G`       | 转到行号              |
| `Ctrl+Shift+O` | 转到符号（函数/变量） |
| `F12`          | 跳转到定义            |
| `Alt+F12`      | 速览定义（不跳转）    |
| `Ctrl+-`       | 返回上一位置          |
| `Ctrl+Shift+-` | 前进到下一位置        |

### 💻 终端

| 快捷键         | 功能           |
| -------------- | -------------- |
| `` Ctrl+` ``   | 打开/关闭终端  |
| `Ctrl+Shift+`` | 新建终端       |
| `Ctrl+C`       | 复制（终端中） |
| `Ctrl+V`       | 粘贴（终端中） |

### 🪟 窗口与布局

| 快捷键         | 功能                      |
| -------------- | ------------------------- |
| `Ctrl+\`       | 拆分编辑器                |
| `Ctrl+1/2/3`   | 切换到第 1/2/3 个编辑器组 |
| `Ctrl+B`       | 切换侧边栏显示            |
| `Ctrl+J`       | 切换面板显示              |
| `Ctrl+Shift+F` | 全局搜索                  |
| `Ctrl+Shift+G` | 源代码管理                |

### 🎨 其他

| 快捷键                | 功能                   |
| --------------------- | ---------------------- |
| `Ctrl+Shift+P` / `F1` | 命令面板               |
| `Ctrl+K Ctrl+S`       | 快捷键设置             |
| `Ctrl+K Ctrl+T`       | 更换主题               |
| `F8`                  | 跳转到下一个错误或警告 |
| `Ctrl+Shift+M`        | 显示问题面板           |

---

## 十、实用小技巧

### 10.1 命令面板（Command Palette）

`Ctrl+Shift+P` 是 VSCode 的核心入口：
- 可以执行任何命令
- 可以搜索设置
- 可以安装主题
- 可以运行任务

> 💡 **记忆口诀**：不知道怎么做？先按 `Ctrl+Shift+P` 搜一下！

### 10.2 Zen Mode（禅模式）

专注写作，隐藏所有干扰：
- `Ctrl+K Z` 进入禅模式
- 按两次 `Esc` 退出

### 10.3 速览（Peek）

不想跳转，只想快速看一下：
- `Alt+F12` 速览定义
- `Ctrl+Shift+F12` 速览引用
- 结果会以内联窗口显示，不会离开当前文件

### 10.4 多工作区

同时处理多个不相关的项目：
1. `文件` → `将文件夹添加到工作区...`
2. 可以在同一个窗口管理多个文件夹
3. 保存工作区：`文件` → `将工作区另存为...`

### 10.5 远程开发

VSCode 支持连接到远程服务器：
- **Remote - SSH**：连接到远程 Linux/Windows/Mac 服务器
- **Remote - WSL**：在 Windows 上连接 WSL
- **Remote - Containers**：在 Docker 容器中开发

安装对应的 Remote 插件后，左下角会出现绿色按钮，点击即可连接。

---

## 结语

恭喜你！现在你已经掌握了 VSCode 的核心使用方法。记住：

1. **多用快捷键**：熟练使用快捷键能大幅提升效率
2. **善用命令面板**：`Ctrl+Shift+P` 能解决大部分问题
3. **按需安装插件**：不要一次性安装太多，需要时再装
4. **保持探索**：VSCode 功能非常丰富，遇到新需求时多搜索

祝你编码愉快！🎉

---

## 参考资源

- [VSCode 官方文档](https://code.visualstudio.com/docs)
- [VSCode 快捷键参考](https://code.visualstudio.com/shortcuts/keyboard-shortcuts-windows.pdf)
- [Awesome VSCode 插件列表](https://github.com/viatsko/awesome-vscode)
