---
words:
  2026-03-03: 1876
---
# GitHub与VSCode零基础实战教程 🐙 + 💻

在自学计算机的过程中，GitHub 是你的"代码备份站"，也是你向未来的雇主展示实力的"作品集"。本教程教你如何在 VS Code 中不使用复杂命令，也能玩转 GitHub。

---

## 一、 准备工作（一次性配置）

1. **安装 Git**：
   - 如果你还没安装，去 [git-scm.com](https://git-scm.com/) 下载并安装。安装时一路点击"Next"即可。
2. **配置你的"名片"**（非常重要）：
   - 安装后，打开 VS Code 的终端（Terminal -> New Terminal），输入以下命令并回车（替换为你自己的信息）：
     ```bash
     git config --global user.name "你的名字"
     git config --global user.email "你的邮箱"
     ```
   - *这决定了 GitHub 上显示的提交者是谁。*
3. **在 VS Code 中登录 GitHub**：
   - 打开 VS Code，点击左下角的**"人头图标"**（账户）。
   - 选择 **"登录以同步设置 (Sign in to sync settings)"** 或直接点击 **"使用 GitHub 登录"**。
   - 在弹出的浏览器中确认授权。

---


## 二、 核心操作：把代码上传到云端

### 1. 初始化与新建仓库 (Repository)

#### 什么是"初始化仓库"？
*初始化仓库（Initialize Repository）相当于在当前文件夹创建一个"版本管理跟踪器"，Git 开始记录这个文件夹里所有文件的变化历史。*

**具体操作**：
- 打开 VS Code，点击左侧活动栏的 **"源代码管理"** 图标（看起来像一个分叉的树）。
- 点击 **"初始化仓库 (Initialize Repository)"**。
- 此时 VS Code 会在你的项目文件夹中创建一个隐藏的 `.git` 文件夹，用来存储所有的版本历史记录。
- *这只是在本地创建仓库，还没有上传到云端。*

#### 发布到 GitHub（上传到云端）
*这是在 GitHub 上为你现在的学习项目开辟一块空间。*

- 初始化完成后，点击 **"发布到 GitHub (Publish to GitHub)"**。
- **如何更改仓库名称？**：
  - VS Code 会弹出一个输入框，默认使用当前文件夹的名称作为仓库名。
  - **直接输入你想要的新名称**（例如：将 `MyProject` 改为 `CS61A-Notes`），然后回车。
  - 你也可以在发布后，去 GitHub 网页端的仓库设置中修改名称。
- 选择 **"发布到 GitHub 公共存储库 (Publish to GitHub public repository)"**（公开，任何人可见）或私有仓库。
- 这样，你在本地写的代码就会在云端生成一个备份。

> 💡 **小贴士**：发布前建议先添加 `.gitignore` 文件，避免把不需要的文件（如缓存、密钥）传上去。

### 2. 每日工作流：三步走 (Add -> Commit -> Push)
*每当你写完一天的代码或笔记，执行以下操作：*

1. **暂存 (Add)**：在"源代码管理"视图中，点击更改文件旁边的 **`+`** 号。
2. **提交 (Commit)**：在顶部的输入框写下你今天做了什么（例如："完成 CS61A 第1课笔记"），然后点击 **"提交 (Commit)"** 按钮。
3. **推送 (Push)**：点击 **"同步更改 (Sync Changes)"** 按钮。
   - *现在，你的代码已经安全地躺在云端了！*

#### ❓ 常见问题：Committing changes 时间很长是正常的吗？

**答案是：视情况而定，有时是正常的。**

| 情况 | 是否正常 | 说明 |
|------|---------|------|
| **首次提交或文件很多** | ✅ 正常 | 首次上传时，Git 需要处理所有文件，可能会显示"Committing changes"转圈几十秒到几分钟 |
| **文件体积很大**（如视频、数据集） | ✅ 正常 | 大文件上传慢是正常的，建议用 [Git LFS](https://git-lfs.github.com/) 管理大文件 |
| **网络连接较慢** | ✅ 正常 | 国内访问 GitHub 有时不稳定，可以尝试开启代理或耐心等待 |
| **超过 5 分钟仍无响应** | ❌ 可能异常 | 可能是网络中断，检查网络后重试 |

**💡 加速小技巧**：
- 如果卡住不动，可以点击左下角进度条旁边的 **"取消"** 按钮，然后重新点击同步
- 避免一次性提交上千个文件，可以分批提交
- 检查是否误选了不需要的大文件（如 `node_modules`、`__pycache__` 等缓存文件夹）

---

## 三、 手机端学习：无电脑时的骚操作

GitHub 的强大之处在于其移动端。

1. **安装 GitHub Mobile App**：在手机应用商店下载 GitHub 官方 App。
2. **查看笔记**：在公交或排队时，打开 App 进入你的仓库，直接阅读你之前写的 `.md` 学习计划和笔记。
3. **"追更"自学指南**：
   - 手机搜索 `PKUFlyingPig/cs-self-learning`。
   - 点击 **Star**（收藏），以后在手机上随时翻阅它的更新。

---

## 四、 如何"搬运"大佬的代码 (Clone)
*你想把《CS自学指南》里的示例代码下载到本地练习：*

1. 在 GitHub 网页端复制项目的 URL（点击绿色按钮 `Code`）。
2. 在 VS Code 中，按下 `Ctrl + Shift + P` (Mac: `Cmd + Shift + P`)。
3. 输入 `Git: Clone`。
4. 粘贴 URL，选择本地存放的文件夹。
5. *完成！你现在可以本地运行大佬的代码了。*

---

## 五、 进阶：如何向《自学指南》项目做贡献 (Fork & PR)
*当你发现指南中有错别字，或者想推荐好课时，可以这样做：*

1. **Fork**：在浏览器打开 [cs-self-learning](https://github.com/PKUFlyingPig/cs-self-learning) 页面，点击右上角的 **Fork**。这会在你自己的账号下创建一个"分身"项目。
2. **修改**：在 VS Code 中 Clone 你自己账号下的那个"分身"项目，进行修改。
3. **提交**：按照前面的"三步走"流程，将修改推送（Push）到你的 GitHub 账号。
4. **Pull Request (PR)**：回到你 GitHub 的"分身"页面，点击 **"Open Pull Request"**。
   - 这样，原作者就能看到你的修改并决定是否合并到官方指南中。
   - *一旦被合并，你就是这个 70k+ Star 项目的贡献者了！*

---

## 六、 给小白的温馨提示

- **不要害怕报错**：Git 的报错通常是因为云端和本地的版本不一致。遇到问题点击 VS Code 弹窗提示的"Pull (拉取)"通常能解决。
- **.gitignore**：如果你发现一些杂乱的缓存文件（如 `.DS_Store` 或 `__pycache__`）也被上传了，记得搜索一下 `.gitignore` 怎么用，它们不应该出现在 GitHub 上。
- **处理"合并冲突"**：如果你在手机上改了笔记，又在电脑上改了同一行，同步时会报错。VS Code 会变红并显示"合并冲突"。
  - 别慌！VS Code 会给出选项：**"保留当前更改"**、**"保留传入更改"**或**"保留两者"**。点一下你想要的，然后重新提交即可。
- **每天同步**：养成习惯，每天关掉电脑前点一下"同步更改"，这能让你在任何地方都能无缝衔接学习。

---
## 七、 实战技巧：如何利用 GitHub 深度自学

1. **利用 Search 找作业参考**：
   - 在 GitHub 搜索框输入 `CS61A homework`。
   - 你能看到全世界学生提交的作业。*注意：仅用于学习思路，不要直接抄袭！*
2. **利用 Issue 找答案**：
   - 在 `PKUFlyingPig/cs-self-learning` 项目的 **Issues** 选项卡里，搜索你遇到的环境配置问题。
   - 很多学长学姐已经踩过坑并给出了方案。
3. **利用 GitHub Pages 部署笔记**：
   - 等你学到 Web 开发阶段，你可以把你的 `.md` 笔记自动变成一个漂亮的个人网站，完全免费！

---
*恭喜！你已经迈出了从"学习者"向"开发者"转变的关键一步。*
